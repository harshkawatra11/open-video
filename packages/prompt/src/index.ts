/** @openvideo/prompt — prompt→PRD template engine (PRD §16). */

export {
  PLACEHOLDER_MARKER,
  DEFAULT_REFINE_TEMPLATE,
  loadRefineTemplate,
  buildRefineInput,
  isPlaceholderTemplate,
} from "./template.ts";
export type { LoadTemplateOptions, LoadedTemplate } from "./template.ts";
