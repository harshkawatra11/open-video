{
  "name": "@openvideo/agents",
  "version": "0.0.0",
  "private": true,
  "description": "OpenVideo agent layer (doc 05, PRD §9): the Director system prompt that drives the Claude CLI session against the @openvideo/mcp-server tools. Single-agent Director now; the fuller specialist roster is a Milestone-E follow-up.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "license": "Apache-2.0"
}
