{
  "name": "@openvideo/cli-adapter",
  "version": "0.0.0",
  "private": true,
  "description": "OpenVideo Claude CLI adapter: spawn the Claude CLI headless and normalize its stream-json events into the Edit/Session protocol (ADR-0002, Appendix C). The single point of coupling to the CLI.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@openvideo/shared": "workspace:*"
  },
  "license": "Apache-2.0"
}
