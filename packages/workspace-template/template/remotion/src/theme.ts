// Brand kit — filled in by the scaffolder from the project's brand kit / PRD.
// {{THEME_COMMENT}}
export const COLORS = {
  primary: "{{COLOR_PRIMARY}}",
  primary2: "{{COLOR_PRIMARY_2}}",
  accent: "{{COLOR_ACCENT}}",
  accentLight: "{{COLOR_ACCENT_LIGHT}}",
  white: "#FFFFFF",
  cream: "#F5F1E6",
  ink: "#0A1018",
  red: "#E2473B",
  green: "#2FBF71",
  muted: "#9FB0C0",
};

export const FONTS = {
  // System-stack fallbacks; real faces loaded via @font-face in fonts.ts
  display: "{{FONT_DISPLAY_STACK}}",
  body: "{{FONT_BODY_STACK}}",
};

export const FPS = {{FPS}};
export const WIDTH = {{WIDTH}};
export const HEIGHT = {{HEIGHT}};

// Terms that should always get emphasis styling in captions — edit per-brief.
export const EMPHASIS_TERMS: string[] = [{{EMPHASIS_TERMS}}];
